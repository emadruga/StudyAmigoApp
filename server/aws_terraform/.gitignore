# =============================================================================
# Terraform .gitignore
# =============================================================================

# State files (contain sensitive data and infrastructure details)
*.tfstate
*.tfstate.backup
*.tfstate.*.backup

# State directory
.terraform/

# Lock file (optional - some teams commit this, some don't)
# Uncomment the next line if you do NOT want to commit the lock file
# .terraform.lock.hcl

# Variable files with secrets
terraform.tfvars
*.auto.tfvars

# Crash logs
crash.log
crash.*.log

# Plan output files
*.tfplan

# Terraform output captures (may contain IPs and instance IDs)
outputs.txt

# Override files (used for local testing)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
