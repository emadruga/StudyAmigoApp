# Production settings (deployed at root with Docker)
VITE_APP_BASE_PATH=/
# API calls go through the reverse proxy at root
VITE_API_BASE_URL=
# Set to true to show the maintenance landing page (requires rebuild)
VITE_MAINTENANCE_MODE=true
