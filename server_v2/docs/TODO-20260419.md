# TODO — SAv1.5 — 2026-04-19

Estado atual: SAv1.5 rodando em `study-amigo.app` (porta 8082).
Troca de senha por email funciona para `emadruga@gmail.com` (SES sandbox).

---

## 🔴 BLOQUEADORES (sem estes, alunos não conseguem trocar senha)

### 1. Aprovação SES — sair do sandbox
- **Status:** Formulário submetido em 2026-04-18
- **Ação:** Aguardar resposta da AWS (24-48h) em console.aws.amazon.com/support
- **Impacto:** Sem isso, só `emadruga@gmail.com` (verificado) recebe email de reset

### 2. Popular emails dos alunos no v15 admin.db
- **Status:** 63 de 96 usuários têm email; **33 ainda com NULL**
- **Script:** `server_v2/scripts/add_email_to_admin_db.py`
- **CSV:** `server_v2/bases/email_mapping_v1.5.csv` (verificar se cobre os 33 faltantes)
- **Ação:**
  ```bash
  # Ver quem está faltando
  ssh -i ~/.ssh/study-amigo-aws ubuntu@54.152.109.26 \
    "sudo docker exec v15_server python3 -c \"
  import sqlite3
  conn = sqlite3.connect('/app/admin.db')
  rows = conn.execute('''SELECT user_id, username, name FROM users
                         WHERE email IS NULL ORDER BY name''').fetchall()
  for r in rows: print(r)
  \""

  # Completar o CSV e rodar o script
  python server_v2/scripts/add_email_to_admin_db.py \
    --csv server_v2/bases/email_mapping_v1.5.csv \
    --production
  ```
- **Meta:** Zero usuários com email NULL no v15 admin.db

---

## 🟡 TESTES (obrigatórios antes de considerar v15 finalizado)

### 3. Teste end-to-end de troca de senha (após SES sair do sandbox)
- Escolher 2-3 alunos voluntários
- Cada um clica "Esqueceu sua senha?" em `study-amigo.app`
- Digita o email cadastrado → recebe link → redefine senha → loga
- Confirma que cards e histórico de revisão estão intactos

### 4. Smoke test automatizado contra v15
- **Script:** `server_v2/scripts/smoke_test.py`
- **Ação:**
  ```bash
  python server_v2/scripts/smoke_test.py --url http://54.152.109.26:8082
  ```
- **Meta:** 100% pass

### 5. Suite unitária `test_api_v1.5.py`
- **Status:** Arquivo existe? Verificar em `server/` ou `server_v2/scripts/`
- Cobre: login por email, POST /change-password (sucesso, senha errada, senha curta, sem sessão)
- **Ação:**
  ```bash
  python -m unittest server/test_api_v1.5.py -v
  ```
- **Meta:** 100% pass

### 6. Teste de race condition contra v15
- **Script:** `server/test_race_condition/test_frontend_flow.py`
- **Ação:**
  ```bash
  python server/test_race_condition/test_frontend_flow.py --url http://54.152.109.26:8082
  ```
- **Meta:** Sem race conditions

---

## 🟢 MELHORIAS (desejáveis, não bloqueiam o funcionamento)

### 7. Registro de novos alunos para próximo ano letivo
- Aba "Cadastro" está desabilitada (greyed out) — correto para turma atual
- Quando reabrir: reabilitar a aba em `AuthPage.jsx` e testar fluxo completo
- O backend `POST /register` já salva email e valida MX record

### 8. Validar comportamento quando email não está cadastrado
- Cenário: aluno digita email errado em "Esqueceu sua senha?"
- Comportamento atual: mensagem genérica ("Se esse email estiver cadastrado...")
- Confirmar que não vaza informação (não diz "email não encontrado")

### 9. Limpeza de contas duplicadas no v15 admin.db
- 33 usuários sem email incluem provavelmente as contas órfãs (duplicatas descartadas)
- Após popular emails dos canônicos, avaliar se os órfãos devem ser deletados
- Referência: `PLAN_MIGRATE_TO_EMAIL_AUTH.md` § Fase 0 — tabela de duplicatas

### 10. Monitoramento de bounces/complaints SES
- Configuration set `my-first-configuration-set` já configurado
- Verificar se CloudWatch tem alarmes ativos para bounce rate > 5%
- AWS pode suspender envio se bounce rate ultrapassar 10%

---

## 📋 REFERÊNCIAS

| Doc | Conteúdo |
|---|---|
| `server_v2/docs/PLAN_MIGRATE_TO_EMAIL_AUTH.md` | Plano técnico completo (Fases 0-6) |
| `server_v2/docs/STRATEGY_MIGRATE_TO_EMAIL_AUTH.md` | Branching, PRs e regras de proteção |
| `server_v2/docs/NEW_DOCKER_ARRANGEMENT_TO_EMAIL_AUTH.md` | Arranjo Docker paralelo |
| `server_v2/scripts/add_email_to_admin_db.py` | Script de migração de emails |
| `server_v2/bases/email_mapping_v1.5.csv` | Mapeamento email → user_id |
